# Where the API lives. Leave as /api for local development (Vite forwards it to
# http://localhost:5000). In production set the full URL of the deployed API, e.g.
#   VITE_API_URL=https://paisa-pal-api.onrender.com/api
VITE_API_URL=/api
